# BookMark

## How to Start?

_Developers_
Run the following commands to set up the server and client:

1. while in /server, `node index.mjs` and `node db/connection.mjs`
2. while in /client, `npm run start`
